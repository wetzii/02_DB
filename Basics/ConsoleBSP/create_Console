

CREATE TABLE Console (
    CID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    Name VARCHAR(50),
    Hersteller VARCHAR(50),
    ReleaseYear INTEGER
);

CREATE TABLE Game (
GID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
Name VARCHAR(50),
Preis FLOAT,
Genre VARCHAR(30),
CID INTEGER REFERENCES Console(CID)

)
